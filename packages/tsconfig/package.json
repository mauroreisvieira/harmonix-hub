{
  "name": "@harmonix-hub/tsconfig",
  "version": "1.0.2",
  "description": "This package provides TSConfig configurations to ensuring consistency and collaboration across projects",
  "scripts": {
    "format": "prettier . --check --cache --ignore-unknown",
    "clean": "rm -rf dist node_modules"
  },
   "publishConfig": {
    "access": "public"
  }
}
