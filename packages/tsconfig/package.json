{
  "name": "@code-harmony/tsconfig",
  "version": "1.0.0",
  "description": "Incorporate the provided tsconfig settings for robust TypeScript support tailored to your project.",
  "scripts": {
    "format": "prettier . --check --cache",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@code-harmony/prettier": "^1.0.0",
    "prettier": "^3.0.3"
  }
}
